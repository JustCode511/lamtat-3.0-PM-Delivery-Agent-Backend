# --- Core ---
langgraph>=0.2.0              # supervisor + intent-routing graph
mangum>=0.17.0                # ASGI adapter for AWS Lambda
python-dotenv==1.0.1          # loads .env into environment
fastapi==0.115.0              # local API entry point
uvicorn==0.30.6              # runs FastAPI locally
httpx==0.27.2                # HTTP client for Jira/Slack (cross-platform, async-capable)

# --- LLM (local dev) ---
google-generativeai==0.8.3   # Gemini API client
openai>=1.30.0               # OpenAI ChatGPT adapter (set LLM_PROVIDER=openai)

# --- MCP ---
mcp==1.1.2                    # official Model Context Protocol SDK

# --- AWS (used only when APP_ENV=aws; safe to install now) ---
boto3==1.35.24               # DynamoDB, Bedrock, SSM
