# Secrets — NEVER commit
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.claude/
venv/
env/

# Runtime state — never commit (generated at runtime, may contain PII or secrets)
data/sessions/
data/session_summaries/
data/user_memory/
data/conversations.json
data/users.json
data/revoked_tokens.json
data/jobs.json
# data/employees.json, projects.json, allocations.json, skills.json are committed as seed data

# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
